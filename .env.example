DATABASE_URL="file:./dev.db"

# Leave STRIPE_SECRET_KEY unset to run against the built-in simulator.
# Drop in real test-mode keys and the same code paths hit Stripe instead.
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET="whsec_localsimulatorsecret"
NEXT_PUBLIC_APP_URL="http://localhost:3000"
